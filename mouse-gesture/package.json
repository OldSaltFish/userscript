{
  "name": "mouse-gesture",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rsbuild build",
    "check": "biome check --write",
    "dev": "rsbuild dev --open",
    "format": "biome format --write",
    "preview": "rsbuild preview",
    "release": "rsbuild build && pwsh -file ./release/release.ps1"
  },
  "dependencies": {
    "solid-js": "^1.9.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@rsbuild/core": "^1.3.15",
    "@rsbuild/plugin-babel": "^1.0.5",
    "@rsbuild/plugin-less": "^1.2.4",
    "@rsbuild/plugin-solid": "^1.0.5",
    "typescript": "^5.8.3"
  }
}
